version: 1.0
phases:
  install:
    commands:
      - npm update -g npm

  pre_build:
    commands:
      - npm install

  build:
    commands:
      - npm run audit:dependencies
      - npm run test
      - npm run build
